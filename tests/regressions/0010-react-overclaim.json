{
  "id": "0010-react-overclaim",
  "title": "Concept components (Database/Config/Auth) must not drive a reuse recommendation",
  "question": "Which projects duplicate configuration loading?",
  "required_scope": "workspace",
  "expected": {
    "objective": "similarity"
  },
  "forbidden": ["recommend reusing configuration", "realistic shared-code recommendation: configuration"],
  "required_evidence": [],
  "note": "Configuration/Database/Authentication are concepts, not evidence. A similarity question must not recommend reuse purely from shared concept components. (The seeded workspace has no strong shared code, so the honest answer may name none — that is correct.)"
}
