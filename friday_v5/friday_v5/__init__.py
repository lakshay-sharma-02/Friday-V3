"""Friday V5 — Claude Code engine, vault memory, local voice, terminal HUD.

The engine is a persistent Claude Code session. Skills are plain
``.claude/skills/<name>/SKILL.md`` files Claude pulls in on demand.
Memory is a vault of linked markdown — no database.
"""

__version__ = "0.1.0"
